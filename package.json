{
  "name": "super-duper-octo-waddle",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "predev": "pnpm install --frozen-lockfile",
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "format": "pnpm -r run format",
    "premigrate": "pnpm install --frozen-lockfile",
    "migrate": "node scripts/ensure-env.mjs && pnpm --filter server run migrate",
    "preseed": "pnpm install --frozen-lockfile",
    "seed": "node scripts/ensure-env.mjs && pnpm --filter server run seed"
  },
  "devDependencies": {
    "@types/node": "20.11.19",
    "typescript": "5.4.3"
  }
}
